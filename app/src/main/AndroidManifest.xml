<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.minhky.go4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".restaurant.ResMaterial"
            android:label="@string/title_activity_res_material"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".restaurant.ResChef" />
        <activity android:name=".restaurant.ResCashier" />
        <activity android:name=".admin.AdminActivity" />
        <activity android:name=".restaurant.ResManActivity" />
        <activity android:name=".login.AccountLogin" />
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

</manifest>